/**
 * @interface AudioPlayerPort
 * connect(guildId: string, voiceChannel: any): Promise<void>
 * subscribe(guildId: string, resource: any): Promise<void>
 * pause(guildId: string): Promise<boolean>
 * resume(guildId: string): Promise<boolean>
 * stop(guildId: string): Promise<void>
 * onStatus(guildId: string, cb: (status: string) => void): void
 */


